parameters:
  sourcesDir: ''
  pathsToDelete: []

steps:
  - ${{ each crtPath in parameters.pathsToDelete }}:
    - script: |
        rm -rf ${{ crtPath }}
      displayName: 'Sanitize path ${{ crtPath }}'
      workingDirectory: ${{ parameters.sourcesDir }}
